name: flutter_code_generators
description: Getters and setters to save variables to and from Map
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  build:
  source_gen:
  watcher: ^1.1.0

dev_dependencies:
  build_test:
  build_runner: ^2.4.9
  test: ^1.25.4